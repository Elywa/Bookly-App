import 'package:bookly_app/Core/utils/styles.dart';
import 'package:flutter/material.dart';

class RatingPeople extends StatelessWidget {
  const RatingPeople({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      '(254)',
      style: Styles.textStyle14,
    );
  }
}